apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mh-workspace
  namespace: migration-harness
  labels:
    app: migration-harness
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
