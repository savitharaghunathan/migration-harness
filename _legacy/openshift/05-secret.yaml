apiVersion: v1
kind: Secret
metadata:
  name: mh-credentials
  namespace: migration-harness
  labels:
    app: migration-harness
type: Opaque
stringData:
  # API key for custom_testinglite provider (update with your actual key)
  CUSTOM_TESTINGLITE_API_KEY: "update-with-your-actual-key"
